import {advertiseMetadata} from "../lib/static-page-metadata.ts";

export const metadata=advertiseMetadata;

export default function AdvertiseLayout({children}:{children:React.ReactNode}){return children}
