import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{contactEmail,PendingTransparencyPage,PolicyShell,publicAddress,publicPhone}from"../../components/TransparencyPage";import{transparencyMetadata}from"../../lib/transparency";
export async function generateMetadata({params}:{params:Promise<{language:string}>}):Promise<Metadata>{const{language}=await params;return transparencyMetadata(language==="dv"?"dv":"en","contact")}
export default async function Contact({params}:{params:Promise<{language:string}>}){const{language}=await params;if(language!=="en"&&language!=="dv")notFound();if(language==="dv")return <PendingTransparencyPage language="dv" title="Contact"/>;const email=<a href={`mailto:${contactEmail}`}>{contactEmail}</a>;return <PolicyShell language="en" title="Contact KuruFeetha" eyebrow="CONTACT"><p>Use the appropriate contact route below. Do not send passwords or private account credentials.</p><h2>General enquiries</h2><p>{email}</p><h2>Editorial and news tips</h2><p>{email}</p><h2>Corrections</h2><p>{email} · <Link href="/en/corrections">Read the corrections policy</Link></p><h2>Advertising</h2><p>{email}</p><h2>Privacy and data questions</h2><p>{email}</p><h2>Phone</h2><p><a href="tel:+9609781818">{publicPhone}</a></p><h2>Address</h2><address>{publicAddress}</address></PolicyShell>}
